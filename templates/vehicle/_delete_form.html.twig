<form method="post" action="{{ path(constant('App\\Controller\\VehicleController::ROUTE_DELETE'), {'id': vehicle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
