{% extends 'base.html.twig' %}

{% block title %}New Vehicle{% endblock %}

{% block body %}
    <div class="col">
        <div class="d-flex align-items-center mb-3">
            <h1>Create new Vehicle</h1>
            <a class="btn btn-primary" style="margin-left: auto" href="{{ path(constant('App\\Controller\\VehicleController::ROUTE_INDEX')) }}">Back to list</a>
        </div>

        {{ include('vehicle/_form.html.twig') }}
    </div>
{% endblock %}
